<UserControl x:Class="BinTreeVisualization.UI.BinTreeControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:BinTreeVisualization.UI" xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800"
             Background="{DynamicResource MaterialDesignPaper}"
             FontFamily="{DynamicResource MaterialDesignFont}"
             >
    <Grid>
        <Canvas Name="MainCanvas"
                             HorizontalAlignment="Center"
                             VerticalAlignment="Top"
                Margin="-70,0,0,0">

            <Canvas.RenderTransform>
                <ScaleTransform x:Name="CanvasScale" CenterX="0" CenterY="0" ScaleX="1" ScaleY="1" />
            </Canvas.RenderTransform>
        </Canvas>
        <StackPanel Name="ProgressStackPanel" Panel.ZIndex="1000" Canvas.Top="0" Canvas.Right="0" HorizontalAlignment="Right" VerticalAlignment="Top"  Width="400" Height="250"
                    >
        </StackPanel>
    </Grid>

</UserControl>
